# Database
# 本地开发
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/whenwefree?schema=public"

# 生产环境 (推荐添加连接池参数)
# DATABASE_URL="postgresql://user:password@host:5432/database?schema=public&connection_limit=10&pool_timeout=30&connect_timeout=10"

# 跨国访问优化建议:
# - connection_limit: 限制连接数 (推荐: 5-20)
# - pool_timeout: 获取连接的超时时间,秒 (推荐: 20-60)
# - connect_timeout: 初始连接超时,秒 (推荐: 10-30)
# - statement_timeout: SQL 语句超时,毫秒 (可选: 30000)
# 
# 使用 Supabase 示例:
# DATABASE_URL="postgresql://postgres:[password]@[project-ref].supabase.co:5432/postgres?pgbouncer=true&connection_limit=10"
#
# 使用 PlanetScale 示例 (MySQL):
# DATABASE_URL="mysql://[username]:[password]@[host]/[database]?ssl={"rejectUnauthorized":true}&connect_timeout=10"

